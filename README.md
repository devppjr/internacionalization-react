# React Internacionalization

This application is a demonstration of the Internationalization in React of students from UFSC.

### Run

| action               | command       | 
|----------------------|---------------|
| install dependencies | `npm install` |
| run project          | `npm start`   |
